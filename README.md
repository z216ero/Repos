## 📦 Base Frontend Template (Vite + React + TS)

Шаблон для быстрого старта SPA-приложений в формате **админка + клиентское приложение**.

- **Современный стек**
- **Минимум магии — максимум контроля**
- **Подходит для масштабирования и командной работы**

---

## 🧱 Стек

- **Vite**
- **React 18+**
- **TypeScript (strict)**
- **React Router**
- **React Query**
- **ESM**
- **pnpm**

---

## 📚 Содержание

- [📁 Структура проекта](#-структура-проекта)
- [📌 Правила размещения кода](#-правила-размещения-кода)
- [🔐 Авторизация](#-авторизация)
- [🧭 Роутинг](#-роутинг)
- [🚀 Запуск](#-запуск)
- [🎯 Цель шаблона](#-цель-шаблона)

---

## 📁 Структура проекта

```text
src/
├─ app/                # Инициализация приложения
│  ├─ App.tsx          # Корневой компонент
│  ├─ providers/       # Глобальные провайдеры (auth, query и т.д.)
│  └─ routes/          # Роутинг и guards
│
├─ pages/              # Страницы (роутовые компоненты)
│  ├─ admin/           # Админская зона
│  │  ├─ AdminLayout.tsx
│  │  └─ AdminDashboardPage.tsx
│  │
│  ├─ client/          # Клиентская зона
│  │  ├─ ClientLayout.tsx
│  │  └─ ClientHomePage.tsx
│  │
│  └─ auth/            # Авторизация
│     └─ LoginPage.tsx
│
├─ shared/             # Переиспользуемые части
│  ├─ ui/              # UI-компоненты (кнопки, инпуты и т.п.)
│  ├─ lib/             # Утилиты, хелперы
│  └─ config/          # Конфиги, константы
│
└─ index.css           # Глобальные стили
```

---

## 📌 Правила размещения кода

### `app/`

В `app/` **не добавлять бизнес-логику**. Только:

- роутинг
- провайдеры
- инициализация приложения

Примеры:

- `AuthProvider`
- `RequireAuth`
- `AppRouter`

### `pages/`

Только страницы.

- **Один page = один route**
- Допускается локальная логика, но без переиспользуемых компонентов

Не класть сюда:

- кнопки
- модалки
- хелперы

Только то, что реально является страницей.

### `shared/`

Всё, что может использоваться в нескольких местах:

- UI
- utils / helpers
- hooks
- api-клиенты

Если сомневаешься, куда класть — клади в `shared/`.

---

## 🔐 Авторизация

Сейчас используется упрощённая auth-модель:

- `isAuth` хранится в **React Context**
- `RequireAuth` защищает **admin-роуты**
- без хранения в `localStorage`

⚠️ Это **архитектурный каркас**, не production-auth.  
JWT / refresh / роли добавляются позже **без переделки структуры**.

---

## 🧭 Роутинг

- `/admin/*` — защищённая админка
- `/app/*` — клиентская часть
- `/login` — авторизация

Каждая зона имеет свой layout.

---

## 🚀 Запуск

```bash
pnpm install
pnpm dev
```

---

## 🎯 Цель шаблона

- Быстро стартовать новые проекты
- Не тратить время на настройку
- Иметь единый стандарт структуры

В перспективе легко превращается в CLI: `create-base-template`.

